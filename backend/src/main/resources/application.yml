server:
  port: ${PORT:8080}

spring:
  application:
    name: studybook
  datasource:
    url: jdbc:postgresql://localhost:5433/studybook
    username: studybook
    password: studybook
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 200MB

studybook:
  cors:
    allowed-origin: http://localhost:5174
  pipeline:
    python-executable: ../.venv/Scripts/python.exe
    script-path: ../prototype/main.py
    upload-dir: ../prototype/samples/uploads
    output-dir: ../prototype/output
    provider: ollama
    model-size: small
    transcription-engine: auto
    timeout-minutes: 10
